[package]
name = "cts_runner"
version.workspace = true
authors = ["Luca Casonato <hello@lcas.dev>"]
edition.workspace = true
description = "CTS runner for wgpu"
license.workspace = true
publish = false

[features]
# Enable the wasm-bindgen code path outside of wasm32.
wasm-bindgen = []

[dependencies]
env_logger.workspace = true

# We make all dependencies conditional on not being wasm,
# so the whole workspace can built as wasm.
[target.'cfg(not(any(target_arch = "wasm32", feature = "wasm-bindgen")))'.dependencies]
deno_console.workspace = true
deno_core.workspace = true
deno_features.workspace = true
deno_url.workspace = true
deno_web.workspace = true
deno_webidl.workspace = true
deno_webgpu.workspace = true
pico-args.workspace = true
tokio = { workspace = true, features = ["full"] }
termcolor.workspace = true

[dev-dependencies]
tempfile.workspace = true
