[package]
name = "wgpu-example-custom-backend"
edition = "2021"
rust-version = "1.93"
publish = false

[features]
default = ["web"]
web = ["wgpu/web"]
# Enable the wasm-bindgen code path outside of wasm32.
wasm-bindgen = ["wgpu/wasm-bindgen"]

[dependencies]
wgpu = { version = "28.0.0", features = [
    "custom",
    "wgsl",
], default-features = false }
pollster = { version = "0.4", features = ["macro"] }
